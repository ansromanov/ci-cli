[project]
name = "ci-cli"
version = "0.1.0"
description = "The one tool to manage all your build pipelines"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.4.1",
    "ruff>=0.12.0",
    "typer>=0.16.0",
    "typing>=3.10.0.0",
]
